# Create this manually — never commit real secrets to git.
# kubectl create secret generic pricehop-secrets \
#   --namespace pricehop \
#   --from-literal=SECRET_KEY='<your-value>' \
#   --from-literal=JWT_SECRET='<your-value>' \
#   --from-literal=GOOGLE_CLIENT_ID='<your-value>' \
#   --from-literal=GOOGLE_CLIENT_SECRET='<your-value>'
#
# The template below is for reference only:
apiVersion: v1
kind: Secret
metadata:
  name: pricehop-secrets
  namespace: pricehop
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
